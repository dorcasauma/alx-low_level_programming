/**
 * set_string - Sets the value of a pointer to a char.
 *
 * @s:a pointer to a pointer to a char
 * @to: a pointer to a char containing the value to set
 */
void set_string(char **s, char *to)
{
*s = to;
}
