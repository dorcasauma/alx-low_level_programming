#include <stdio.h>
#include <ctype.h>

/**
 * _isupper - checks is a character is uppercase.
 * @c character to be checked.
 * @return: zero value
 */
int _isupper(int c)
{
if (isupper(c))
return (1);
else
{
return (0);
}
}
